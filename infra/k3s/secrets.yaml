# Secrets template — DO NOT commit actual values
# On the VPS, create the real secret:
#   kubectl -n aweborn create secret generic aweborn-secrets \
#     --from-literal=STRIPE_SECRET_KEY=sk_live_... \
#     --from-literal=OPENAI_API_KEY=sk-...

apiVersion: v1
kind: Secret
metadata:
  name: aweborn-secrets
  namespace: aweborn
type: Opaque
stringData:
  # Replace these placeholder values before applying
  STRIPE_SECRET_KEY: "REPLACE_ME"
  # Future: API keys for Gen AI services (Phase 07)
  # OPENAI_API_KEY: "REPLACE_ME"
  # MESHY_API_KEY: "REPLACE_ME"
